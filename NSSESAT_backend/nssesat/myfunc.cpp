// myfunc.cpp: ?????`?????C???v???????e?[?V????
#include "myfunc.h"

// ?m??????????^?U?????
// prob : ?m??
// return : ?m?????????C?????_????true?????false
bool flip(float prob) {
	if((prob == 1.0) || (myrandom() <= prob))
		return true;
    else
		return false;
}

// ????????
// return : 0?`1?????
float myrandom() {
	return (float)rand() / (float)RAND_MAX; 
}

// ????????
// i : ????????l
// return : 0?`i?????????
int randomInt(int i) {
	if(i < 0)
		return 0;
	else
		return rand() % (i+1);
}

// ?P??O???]
short reverse(short i) {
	if(i == 0)
		return 1;
	else if(i == 1)
		return 0;
	else
		return -1;
}
